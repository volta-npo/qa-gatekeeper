openapi: 3.1.0
info:
  title: "QA Gatekeeper API"
  version: "1.0.0"
  description: "Backend API for A CPG-style pre-handoff QA checklist for student-built deliverables."
servers:
  - url: http://127.0.0.1:8788
    description: Rust backend
paths:
  /health:
    get:
      summary: Health check
      responses:
        '200':
          description: Product identity and health status
  /score:
    get:
      summary: Score deterministic sample gates
      responses:
        '200':
          description: Release score and fingerprint
 