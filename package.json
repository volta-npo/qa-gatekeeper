{
  "name": "@volta-oss/qa-gatekeeper",
  "version": "3.0.0",
  "private": false,
  "type": "module",
  "description": "A CPG-style pre-handoff QA checklist for student-built deliverables.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test test/*.test.js",
    "start": "python3 -m http.server 4173",
    "validate": "npm test",
    "certify": "npm run build && node --test test/v3.test.js",
    "test:all": "npm test",
    "test:rust": "cargo test --manifest-path backend/rust/Cargo.toml",
    "test:backend": "npm run test:rust",
    "clean": "rm -f src/*.js src/*.js.map src/*.d.ts test/*.js test/*.js.map test/*.d.ts",
    "ci": "npm ci && npm test && npm run test:backend --if-present",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "docker:build": "docker build -t volta-oss/qa-gatekeeper:local .",
    "docker:run": "docker compose up --build",
    "serve:rust": "cargo run --manifest-path backend/rust/Cargo.toml -- serve"
  },
  "keywords": [
    "digital-equity",
    "local-first",
    "oss",
    "student agency operations",
    "release",
    "volta"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/volta-npo/qa-gatekeeper.git"
  },
  "homepage": "https://volta-npo.github.io/qa-gatekeeper/",
  "bugs": {
    "url": "https://github.com/volta-npo/qa-gatekeeper/issues"
  },
  "author": "Volta Open Source <volta.npo@gmail.com>",
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "@types/node": "^26.4.0"
  }
}
